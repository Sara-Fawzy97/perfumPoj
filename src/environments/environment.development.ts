import { Environment } from './environment';

export const environment: Environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api/v1',
  appName: 'PerfumPoj Storefront (Dev)',
  enableMockFallback: true
};
